# Extensions

Our version of the Lua API has LuaJIT extensions. You can read about it on the official site:

{% embed url="https://luajit.org/extensions.html" %}



Please note that `ffi` and `package` libraries are disabled until explicitly enabled by the "Allow Unsafe" feature as well as `io` .
