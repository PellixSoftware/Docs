# CBasePlayerController

```lua
controller:get_pawn(): entity | nil
```

Returns a new entity or cached one from the controller. If new was created, whole class memory is cached immediately.



```lua
controller:get_player_name(): string
```

Returns name of the player.
